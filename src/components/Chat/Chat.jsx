import React from "react";

const Chat = ({ className }) => {
  return (
    <div
      className={`${className} border-r-[1px] border-l-[1px] border-[#dddddd35]`}
    >
      Chat
    </div>
  );
};

export default Chat;
